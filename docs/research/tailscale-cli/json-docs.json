{"Name":"tailscale","Desc":"For help on subcommands, add --help after: \"tailscale status --help\".\n\nThis CLI is still under active development. Commands and flags will\nchange in the future.","Subcommands":[{"Name":"up","Desc":"\"tailscale up\" connects this machine to your Tailscale network,\ntriggering authentication if necessary.\n\nWith no flags, \"tailscale up\" brings the network online without\nchanging any settings. (That is, it's the opposite of \"tailscale\ndown\").\n\nIf flags are specified, the flags must be the complete set of desired\nsettings. An error is returned if any setting would be changed as a\nresult of an unspecified flag's default value, unless the --reset flag\nis also used. (The flags --auth-key, --force-reauth, and --qr are not\nconsidered settings that need to be re-specified when modifying\nsettings.)","Flags":[{"Name":"accept-dns","Desc":"accept DNS configuration from the admin panel"},{"Name":"accept-risk","Desc":"accept risk and skip confirmation for risk types: lose-ssh,mac-app-connector,all"},{"Name":"accept-routes","Desc":"accept routes advertised by other Tailscale nodes"},{"Name":"advertise-connector","Desc":"advertise this node as an app connector"},{"Name":"advertise-exit-node","Desc":"offer to be an exit node for internet traffic for the tailnet"},{"Name":"advertise-routes","Desc":"routes to advertise to other nodes (comma-separated, e.g. \"10.0.0.0/8,192.168.0.0/24\") or empty string to not advertise routes"},{"Name":"advertise-tags","Desc":"comma-separated ACL tags to request (e.g. \"tag:eng,tag:montreal,tag:ssh\"); the \"tag:\" prefix is optional and added automatically when omitted (e.g. \"eng,montreal,ssh\")"},{"Name":"audience","Desc":"Audience used when requesting an ID token from an identity provider for auth keys via workload identity federation"},{"Name":"auth-key","Desc":"node authorization key; if it begins with \"file:\", then it's a path to a file containing the authkey"},{"Name":"client-id","Desc":"Client ID used to generate authkeys via workload identity federation"},{"Name":"client-secret","Desc":"Client Secret used to generate authkeys via OAuth; if it begins with \"file:\", then it's a path to a file containing the secret"},{"Name":"exit-node","Desc":"Tailscale exit node (IP, base name, or auto:any) for internet traffic, or empty string to not use an exit node"},{"Name":"exit-node-allow-lan-access","Desc":"allow direct access to the local network when routing traffic via an exit node"},{"Name":"force-reauth","Desc":"force reauthentication (WARNING: this may bring down the Tailscale connection and thus should not be done remotely over SSH or RDP)"},{"Name":"hostname","Desc":"hostname to use instead of the one provided by the OS"},{"Name":"id-token","Desc":"ID token from the identity provider to exchange with the control server for workload identity federation; if it begins with \"file:\", then it's a path to a file containing the token"},{"Name":"json","Desc":"output in JSON format (WARNING: format subject to change)"},{"Name":"login-server","Desc":"base URL of control server"},{"Name":"operator","Desc":"Unix username to allow to operate on tailscaled without sudo"},{"Name":"qr","Desc":"show QR code for login URLs"},{"Name":"qr-format","Desc":"QR code formatting (auto, ascii, large, small)"},{"Name":"report-posture","Desc":"allow management plane to gather device posture information"},{"Name":"reset","Desc":"reset unspecified settings to their default values"},{"Name":"shields-up","Desc":"don't allow incoming connections"},{"Name":"ssh","Desc":"run an SSH server, permitting access per tailnet admin's declared policy"},{"Name":"timeout","Desc":"maximum amount of time to wait for tailscaled to enter a Running state; default (0s) blocks forever"}]},{"Name":"down","Desc":"Disconnect from Tailscale","Flags":[{"Name":"accept-risk","Desc":"accept risk and skip confirmation for risk types: lose-ssh,mac-app-connector,all"},{"Name":"reason","Desc":"reason for the disconnect, if required by a policy"}]},{"Name":"set","Desc":"\"tailscale set\" allows changing specific preferences.\n\nUnlike \"tailscale up\", this command does not require the complete set of desired settings.\n\nOnly settings explicitly mentioned will be set. There are no default values.","Flags":[{"Name":"accept-dns","Desc":"accept DNS configuration from the admin panel"},{"Name":"accept-risk","Desc":"accept risk and skip confirmation for risk types: lose-ssh,mac-app-connector,all"},{"Name":"accept-routes","Desc":"accept routes advertised by other Tailscale nodes"},{"Name":"advertise-connector","Desc":"offer to be an app connector for domain specific internet traffic for the tailnet"},{"Name":"advertise-exit-node","Desc":"offer to be an exit node for internet traffic for the tailnet"},{"Name":"advertise-routes","Desc":"routes to advertise to other nodes (comma-separated, e.g. \"10.0.0.0/8,192.168.0.0/24\") or empty string to not advertise routes"},{"Name":"auto-update","Desc":"automatically update to the latest available version"},{"Name":"exit-node","Desc":"Tailscale exit node (IP, base name, or auto:any) for internet traffic, or empty string to not use an exit node"},{"Name":"exit-node-allow-lan-access","Desc":"allow direct access to the local network when routing traffic via an exit node"},{"Name":"hostname","Desc":"hostname to use instead of the one provided by the OS"},{"Name":"nickname","Desc":"nickname for the current account"},{"Name":"operator","Desc":"Unix username to allow to operate on tailscaled without sudo"},{"Name":"relay-server-port","Desc":"UDP port number (0 will pick a random unused port) for the relay server to bind to, on all interfaces, or empty string to disable relay server functionality"},{"Name":"relay-server-static-endpoints","Desc":"static IP:port endpoints to advertise as candidates for relay connections (comma-separated, e.g. \"[2001:db8::1]:40000,192.0.2.1:40000\") or empty string to not advertise any static endpoints"},{"Name":"report-posture","Desc":"allow management plane to gather device posture information"},{"Name":"shields-up","Desc":"don't allow incoming connections"},{"Name":"ssh","Desc":"run an SSH server, permitting access per tailnet admin's declared policy"},{"Name":"update-check","Desc":"notify about available Tailscale updates"},{"Name":"webclient","Desc":"expose the web interface for managing this node over Tailscale at port 5252"}]},{"Name":"get","Desc":"\"tailscale get\" shows the current value of one or all preferences.\n\nWith no argument or \"all\", all preferences are shown.\nWith a specific setting name, only that value is shown.\n\nThe setting names are the same flag names accepted by \"tailscale set\".","Flags":[{"Name":"json","Desc":"output as JSON"},{"Name":"set-flags","Desc":"output as \"tailscale set\" flag arguments"}]},{"Name":"login","Desc":"\"tailscale login\" logs this machine in to your Tailscale network.\nThis command is currently in alpha and may change in the future.","Flags":[{"Name":"accept-dns","Desc":"accept DNS configuration from the admin panel"},{"Name":"accept-routes","Desc":"accept routes advertised by other Tailscale nodes"},{"Name":"advertise-connector","Desc":"advertise this node as an app connector"},{"Name":"advertise-exit-node","Desc":"offer to be an exit node for internet traffic for the tailnet"},{"Name":"advertise-routes","Desc":"routes to advertise to other nodes (comma-separated, e.g. \"10.0.0.0/8,192.168.0.0/24\") or empty string to not advertise routes"},{"Name":"advertise-tags","Desc":"comma-separated ACL tags to request (e.g. \"tag:eng,tag:montreal,tag:ssh\"); the \"tag:\" prefix is optional and added automatically when omitted (e.g. \"eng,montreal,ssh\")"},{"Name":"audience","Desc":"Audience used when requesting an ID token from an identity provider for auth keys via workload identity federation"},{"Name":"auth-key","Desc":"node authorization key; if it begins with \"file:\", then it's a path to a file containing the authkey"},{"Name":"client-id","Desc":"Client ID used to generate authkeys via workload identity federation"},{"Name":"client-secret","Desc":"Client Secret used to generate authkeys via OAuth; if it begins with \"file:\", then it's a path to a file containing the secret"},{"Name":"exit-node","Desc":"Tailscale exit node (IP, base name, or auto:any) for internet traffic, or empty string to not use an exit node"},{"Name":"exit-node-allow-lan-access","Desc":"allow direct access to the local network when routing traffic via an exit node"},{"Name":"hostname","Desc":"hostname to use instead of the one provided by the OS"},{"Name":"id-token","Desc":"ID token from the identity provider to exchange with the control server for workload identity federation; if it begins with \"file:\", then it's a path to a file containing the token"},{"Name":"login-server","Desc":"base URL of control server"},{"Name":"nickname","Desc":"short name for the account"},{"Name":"operator","Desc":"Unix username to allow to operate on tailscaled without sudo"},{"Name":"qr","Desc":"show QR code for login URLs"},{"Name":"qr-format","Desc":"QR code formatting (auto, ascii, large, small)"},{"Name":"report-posture","Desc":"allow management plane to gather device posture information"},{"Name":"shields-up","Desc":"don't allow incoming connections"},{"Name":"ssh","Desc":"run an SSH server, permitting access per tailnet admin's declared policy"},{"Name":"timeout","Desc":"maximum amount of time to wait for tailscaled to enter a Running state; default (0s) blocks forever"}]},{"Name":"logout","Desc":"\"tailscale logout\" brings the network down and invalidates\nthe current node key, forcing a future use of it to cause\na reauthentication.","Flags":[{"Name":"reason","Desc":"reason for the logout, if required by a policy"}]},{"Name":"switch","Desc":"\"tailscale switch\" switches between logged in accounts. You can\nuse the ID that's returned from 'tailnet switch -list'\nto pick which profile you want to switch to. Alternatively, you\ncan use the Tailnet, account names, or display names to switch as well.\n\nThis command is currently in alpha and may change in the future.","Subcommands":[{"Name":"remove","Desc":"\"tailscale switch remove\" removes a Tailscale account from the\nlocal machine. This does not delete the account itself, but\nit will no longer be available for switching to. You can\nadd it back by logging in again.\n\nThis command is currently in alpha and may change in the future."}],"Flags":[{"Name":"json","Desc":"list available accounts in JSON format"},{"Name":"list","Desc":"list available accounts"}]},{"Name":"configure","Desc":"The 'configure' set of commands are intended to provide a way to enable different\nservices on the host to use Tailscale in more ways.","Subcommands":[{"Name":"kubeconfig","Desc":"Run this command to configure kubectl to connect to a Kubernetes cluster over Tailscale.\n\nThe hostname argument should be set to the Tailscale hostname of the peer running as an auth proxy in the cluster.\n\nSee: https://tailscale.com/s/k8s-auth-proxy","Flags":[{"Name":"http","Desc":"use HTTP instead of HTTPS to connect to the auth proxy. Ignored if you include a scheme in the hostname argument."}]},{"Name":"sysext","Desc":"The sysext set of commands provides a way to activate, deactivate, or manage the state of the Tailscale system extension on macOS. This is only relevant if you are running the Standalone variant of the Tailscale client for macOS. To access more detailed information about system extensions installed on this Mac, run 'systemextensionsctl list'.","Subcommands":[{"Name":"activate","Desc":"This command registers the Tailscale system extension with macOS. To run Tailscale, you'll also need to install the VPN configuration separately (run `tailscale configure vpn-config install`). After running this command, you need to approve the extension in System Settings \u003e Login Items and Extensions \u003e Network Extensions."},{"Name":"deactivate","Desc":"This command deactivates the Tailscale system extension on macOS. To completely remove Tailscale, you'll also need to delete the VPN configuration separately (use `tailscale configure vpn-config uninstall`)."},{"Name":"status","Desc":"This command prints the enablement status of the Tailscale system extension. If the extension is not enabled, run `tailscale sysext activate` to enable it."}]},{"Name":"mac-vpn","Desc":"The vpn-config set of commands provides a way to add or remove the Tailscale VPN configuration from the macOS settings. This is the entry that appears in System Settings \u003e VPN.","Subcommands":[{"Name":"install","Desc":"This command writes the Tailscale VPN configuration to the macOS settings. This is the entry that appears in System Settings \u003e VPN. If you are running the Standalone variant of the client, you'll also need to install the system extension separately (run `tailscale configure sysext activate`)."},{"Name":"uninstall","Desc":"This command removes the Tailscale VPN configuration from the macOS settings. This is the entry that appears in System Settings \u003e VPN. If you are running the Standalone variant of the client, you'll also need to deactivate the system extension separately (run `tailscale configure sysext deactivate`)."}]}]},{"Name":"syspolicy","Desc":"The 'tailscale syspolicy' command provides tools for diagnosing the MDM and system policy configuration.","Subcommands":[{"Name":"list","Desc":"The 'tailscale syspolicy list' subcommand displays the effective policy settings and their sources (e.g., MDM or environment variables).","Flags":[{"Name":"json","Desc":"output in JSON format"}]},{"Name":"reload","Desc":"The 'tailscale syspolicy reload' subcommand forces a reload of policy settings, even if no changes are detected, and prints the result.","Flags":[{"Name":"json","Desc":"output in JSON format"}]}]},{"Name":"netcheck","Desc":"Print an analysis of local network conditions","Flags":[{"Name":"bind-address","Desc":"send and receive connectivity probes using this locally bound IP address; default: OS-assigned"},{"Name":"bind-port","Desc":"send and receive connectivity probes using this UDP port; default: OS-assigned"},{"Name":"every","Desc":"if non-zero, do an incremental report with the given frequency"},{"Name":"format","Desc":"output format; empty (for human-readable), \"json\" or \"json-line\""},{"Name":"verbose","Desc":"verbose logs"}]},{"Name":"ip","Desc":"Show Tailscale IP addresses for peer or service. Peer defaults to the current machine.","Flags":[{"Name":"1","Desc":"only print one IP address"},{"Name":"4","Desc":"only print IPv4 address"},{"Name":"6","Desc":"only print IPv6 address"},{"Name":"assert","Desc":"assert that one of the node's IP(s) matches this IP address"}]},{"Name":"dns","Desc":"The 'tailscale dns' subcommand provides tools for diagnosing the internal DNS\nforwarder (100.100.100.100).\n\nFor more information about the DNS functionality built into Tailscale, refer to\nhttps://tailscale.com/kb/1054/dns.","Subcommands":[{"Name":"status","Desc":"The 'tailscale dns status' subcommand prints the current DNS status and\nconfiguration, including:\n\n- Whether the built-in DNS forwarder is enabled.\n\n- The MagicDNS configuration provided by the coordination server.\n\n- Details on which resolver(s) Tailscale believes the system is using by\n  default.\n\nThe --all flag can be used to output advanced debugging information, including\nfallback resolvers, nameservers, certificate domains, extra records, and the\nexit node filtered set.\n\n=== Contents of the MagicDNS configuration ===\n\nThe MagicDNS configuration is provided by the coordination server to the client\nand includes the following components:\n\n- MagicDNS enablement status: Indicates whether MagicDNS is enabled across the\n  entire tailnet.\n\n- MagicDNS Suffix: The DNS suffix used for devices within your tailnet.\n\n- DNS Name: The DNS name that other devices in the tailnet can use to reach this\n  device.\n\n- Resolvers: The preferred DNS resolver(s) to be used for resolving queries, in\n  order of preference. If no resolvers are listed here, the system defaults are\n  used.\n\n- Split DNS Routes: Custom DNS resolvers may be used to resolve hostnames in\n  specific domains, this is also known as a 'Split DNS' configuration. The\n  mapping of domains to their respective resolvers is provided here.\n\n- Certificate Domains: The DNS names for which the coordination server will\n  assist in provisioning TLS certificates.\n\n- Extra Records: Additional DNS records that the coordination server might\n  provide to the internal DNS resolver.\n\n- Exit Node Filtered Set: DNS suffixes that the node, when acting as an exit\n  node DNS proxy, will not answer.\n\nFor more information about the DNS functionality built into Tailscale, refer to\nhttps://tailscale.com/kb/1054/dns.","Flags":[{"Name":"all","Desc":"outputs advanced debugging information"},{"Name":"json","Desc":"output in JSON format"}]},{"Name":"query","Desc":"The 'tailscale dns query' subcommand performs a DNS query for the specified name\nusing the internal DNS forwarder (100.100.100.100).\n\nBy default, the DNS query will request an A record. Specify the record type as\na second argument after the name (e.g. AAAA, CNAME, MX, NS, PTR, SRV, TXT).\n\nThe output also provides information about the resolver(s) used to resolve the\nquery.","Flags":[{"Name":"json","Desc":"output in JSON format"}]}]},{"Name":"status","Desc":"JSON FORMAT\n\nWarning: this format has changed between releases and might change more\nin the future.\n\nFor a description of the fields, see the \"type Status\" declaration at:\n\nhttps://github.com/tailscale/tailscale/blob/main/ipn/ipnstate/ipnstate.go\n\n(and be sure to select branch/tag that corresponds to the version\n of Tailscale you're running)","Flags":[{"Name":"active","Desc":"filter output to only peers with active sessions (not applicable to web mode)"},{"Name":"browser","Desc":"open a browser in web mode"},{"Name":"header","Desc":"show column headers in table format"},{"Name":"json","Desc":"output in JSON format (WARNING: format subject to change)"},{"Name":"listen","Desc":"listen address for web mode; use port 0 for automatic"},{"Name":"peers","Desc":"show status of peers"},{"Name":"self","Desc":"show status of local machine"},{"Name":"web","Desc":"run webserver with HTML showing status"}]},{"Name":"metrics","Desc":"The 'tailscale metrics' command shows Tailscale user-facing metrics (as opposed\nto internal metrics printed by 'tailscale debug metrics').\n\nFor more information about Tailscale metrics, refer to\nhttps://tailscale.com/s/client-metrics","Subcommands":[{"Name":"print","Desc":"Print current metric values in Prometheus text format"},{"Name":"write","Desc":"The 'tailscale metrics write' command writes metric values to a text file provided as its\nonly argument. It's meant to be used alongside Prometheus node exporter, allowing Tailscale\nmetrics to be consumed and exported by the textfile collector.\n\nAs an example, to export Tailscale metrics on an Ubuntu system running node exporter, you\ncan regularly run 'tailscale metrics write /var/lib/prometheus/node-exporter/tailscaled.prom'\nusing cron or a systemd timer."}]},{"Name":"ping","Desc":"The 'tailscale ping' command pings a peer node from the Tailscale layer\nand reports which route it took for each response. The first ping or\nso will likely go over DERP (Tailscale's TCP relay protocol) while NAT\ntraversal finds a direct path through.\n\nIf 'tailscale ping' works but a normal ping does not, that means one\nside's operating system firewall is blocking packets; 'tailscale ping'\ndoes not inject packets into either side's TUN devices.\n\nBy default, 'tailscale ping' stops after 10 pings or once a direct\n(non-DERP) path has been established, whichever comes first.\n\nThe provided hostname must resolve to or be a Tailscale IP\n(e.g. 100.x.y.z) or a subnet IP advertised by a Tailscale\nrelay node.","Flags":[{"Name":"c","Desc":"max number of pings to send. 0 for infinity."},{"Name":"icmp","Desc":"do a ICMP-level ping (through WireGuard, but not the local host OS stack)"},{"Name":"peerapi","Desc":"try hitting the peer's peerapi HTTP server"},{"Name":"size","Desc":"size of the ping message (disco pings only). 0 for minimum size."},{"Name":"timeout","Desc":"timeout before giving up on a ping"},{"Name":"tsmp","Desc":"do a TSMP-level ping (through WireGuard, but not either host OS stack)"},{"Name":"until-direct","Desc":"stop once a direct path is established"},{"Name":"verbose","Desc":"verbose output"}]},{"Name":"nc","Desc":"Connect to a port on a host, connected to stdin/stdout"},{"Name":"ssh","Desc":"The 'tailscale ssh' command is an optional wrapper around the system 'ssh'\ncommand that's useful in some cases. Tailscale SSH does not require its use;\nmost users running the Tailscale SSH server will prefer to just use the normal\n'ssh' command or their normal SSH client.\n\nThe 'tailscale ssh' wrapper adds a few things:\n\n* It resolves the destination server name in its arguments using MagicDNS,\n  even if --accept-dns=false.\n* It works in userspace-networking mode, by supplying a ProxyCommand to the\n  system 'ssh' command that connects via a pipe through tailscaled.\n* It automatically checks the destination server's SSH host key against the\n  node's SSH host key as advertised via the Tailscale coordination server."},{"Name":"funnel","Desc":"Funnel enables you to share a local server on the internet using Tailscale.\n\nTo share only within your tailnet, use `tailscale serve`\n\n\u003ctarget\u003e can be a file, directory, text, or most commonly the location to a service running on the\nlocal machine. The location to the location service can be expressed as a port number (e.g., 3000),\na partial URL (e.g., localhost:3000), or a full URL including a path (e.g., http://localhost:3000/foo).\nOn Unix-like systems, you can also specify a Unix domain socket (e.g., unix:/tmp/myservice.sock).\n\nEXAMPLES\n  - Expose an HTTP server running at 127.0.0.1:3000 in the foreground:\n    $ tailscale funnel 3000\n\n  - Expose an HTTP server running at 127.0.0.1:3000 in the background:\n    $ tailscale funnel --bg 3000\n\n  - Expose an HTTPS server with invalid or self-signed certificates at https://localhost:8443\n    $ tailscale funnel https+insecure://localhost:8443\n\n  - Expose a service listening on a Unix socket (Linux/macOS/BSD only):\n    $ tailscale funnel unix:/var/run/myservice.sock\n\nFor more examples and use cases visit our docs site https://tailscale.com/kb/1247/funnel-serve-use-cases","Subcommands":[{"Name":"status","Desc":"View current funnel configuration","Flags":[{"Name":"json","Desc":"output JSON"}]},{"Name":"reset","Desc":"Reset current funnel config"}],"Flags":[{"Name":"bg","Desc":"Run the command as a background process (default false, when --service is set defaults to true)."},{"Name":"https","Desc":"Expose an HTTPS server at the specified port (default mode)"},{"Name":"proxy-protocol","Desc":"PROXY protocol version (1 or 2) for TCP forwarding"},{"Name":"set-path","Desc":"Appends the specified path to the base URL for accessing the underlying service"},{"Name":"tcp","Desc":"Expose a TCP forwarder to forward raw TCP packets at the specified port"},{"Name":"tls-terminated-tcp","Desc":"Expose a TCP forwarder to forward TLS-terminated TCP packets at the specified port"},{"Name":"yes","Desc":"Update without interactive prompts (default false)"}]},{"Name":"serve","Desc":"Tailscale Serve enables you to share a local server securely within your tailnet.\n\nTo share a local server on the internet, use `tailscale funnel`\n\n\u003ctarget\u003e can be a file, directory, text, or most commonly the location to a service running on the\nlocal machine. The location to the location service can be expressed as a port number (e.g., 3000),\na partial URL (e.g., localhost:3000), or a full URL including a path (e.g., http://localhost:3000/foo).\nOn Unix-like systems, you can also specify a Unix domain socket (e.g., unix:/tmp/myservice.sock).\n\nEXAMPLES\n  - Expose an HTTP server running at 127.0.0.1:3000 in the foreground:\n    $ tailscale serve 3000\n\n  - Expose an HTTP server running at 127.0.0.1:3000 in the background:\n    $ tailscale serve --bg 3000\n\n  - Expose an HTTPS server with invalid or self-signed certificates at https://localhost:8443\n    $ tailscale serve https+insecure://localhost:8443\n\n  - Expose a service listening on a Unix socket (Linux/macOS/BSD only):\n    $ tailscale serve unix:/var/run/myservice.sock\n\nFor more examples and use cases visit our docs site https://tailscale.com/kb/1247/funnel-serve-use-cases","Subcommands":[{"Name":"status","Desc":"View current serve configuration","Flags":[{"Name":"json","Desc":"output JSON"}]},{"Name":"reset","Desc":"Reset current serve config"},{"Name":"drain","Desc":"Make the current node no longer accept new connections for the specified service.\nExisting connections will continue to work until they are closed, but no new connections will be accepted.\nUse this command to gracefully remove a service from the current node without disrupting existing connections.\n\u003cservice\u003e should be a service name (e.g., svc:my-service)."},{"Name":"clear","Desc":"Remove all handlers configured for the specified service."},{"Name":"advertise","Desc":"Advertise this node as a service proxy to the tailnet. This command is used\nto make the current node be considered as a service host for a service. This is\nuseful to bring a service back after it has been drained. (i.e. after running \n`tailscale serve drain \u003cservice\u003e`). This is not needed if you are using `tailscale serve` to initialize a service."},{"Name":"get-config","Desc":"Get the configuration for services that this node is currently hosting in a\nformat that can later be provided to set-config. This can be used to declaratively set\nconfiguration for a service host.","Flags":[{"Name":"all","Desc":"read config from all services"},{"Name":"service","Desc":"read config from a particular service"}]},{"Name":"set-config","Desc":"Read the provided configuration file and use it to declaratively set the configuration\nfor either a single service, or for all services that this node is hosting. If --service is specified,\nall endpoint handlers for that service are overwritten. If --all is specified, all endpoint handlers for\nall services are overwritten.\n\nFor information on the file format, see tailscale.com/kb/1589/tailscale-services-configuration-file","Flags":[{"Name":"all","Desc":"apply config to all services"},{"Name":"service","Desc":"apply config to a particular service"}]}],"Flags":[{"Name":"accept-app-caps","Desc":"App capabilities to forward to the server (specify multiple capabilities with a comma-separated list)"},{"Name":"bg","Desc":"Run the command as a background process (default false, when --service is set defaults to true)."},{"Name":"http","Desc":"Expose an HTTP server at the specified port"},{"Name":"https","Desc":"Expose an HTTPS server at the specified port (default mode)"},{"Name":"proxy-protocol","Desc":"PROXY protocol version (1 or 2) for TCP forwarding"},{"Name":"service","Desc":"Serve for a service with distinct virtual IP instead on node itself."},{"Name":"set-path","Desc":"Appends the specified path to the base URL for accessing the underlying service"},{"Name":"tcp","Desc":"Expose a TCP forwarder to forward raw TCP packets at the specified port"},{"Name":"tls-terminated-tcp","Desc":"Expose a TCP forwarder to forward TLS-terminated TCP packets at the specified port"},{"Name":"tun","Desc":"Forward all traffic to the local machine (default false), only supported for services. Refer to docs for more information."},{"Name":"yes","Desc":"Update without interactive prompts (default false)"}]},{"Name":"service","Desc":"The 'tailscale service' command groups subcommands for Tailscale Services.\n\nA Tailscale Service is a virtual service with its own IP addresses. Which\nServices this node can reach is determined by the tailnet's ACLs. Use the 'list'\nsubcommand to see the Services currently available to this node.","Subcommands":[{"Name":"list","Desc":"List the Tailscale Services your node can access","Flags":[{"Name":"json","Desc":"output in JSON format"}]}]},{"Name":"version","Desc":"Print Tailscale version","Flags":[{"Name":"daemon","Desc":"also print local node's daemon version"},{"Name":"json","Desc":"output in JSON format"},{"Name":"track","Desc":"which track to check for updates: \"stable\", \"release-candidate\", or \"unstable\" (dev); empty means same as current"},{"Name":"upstream","Desc":"fetch and print the latest upstream release version from pkgs.tailscale.com"}]},{"Name":"web","Desc":"\"tailscale web\" runs a webserver for controlling the Tailscale daemon.\n\nIt's primarily intended for use on Synology, QNAP, and other\nNAS devices where a web interface is the natural place to control\nTailscale, as opposed to a CLI or a native app.","Flags":[{"Name":"cgi","Desc":"run as CGI script"},{"Name":"listen","Desc":"listen address; use port 0 for automatic"},{"Name":"origin","Desc":"origin at which the web UI is served (if behind a reverse proxy or used with cgi)"},{"Name":"prefix","Desc":"URL prefix added to requests (for cgi or reverse proxies)"},{"Name":"readonly","Desc":"run web UI in read-only mode"}]},{"Name":"file","Desc":"Send or receive files","Subcommands":[{"Name":"cp","Desc":"Copy file(s) to a host","Flags":[{"Name":"name","Desc":"alternate filename to use, especially useful when \u003cfile\u003e is \"-\" (stdin)"},{"Name":"targets","Desc":"list possible file cp targets"},{"Name":"update-interval","Desc":"how often to repaint the progress line; zero or negative disables progress display entirely"},{"Name":"verbose","Desc":"verbose output"}]},{"Name":"get","Desc":"Move files out of the Tailscale file inbox","Flags":[{"Name":"conflict","Desc":"`behavior` when a conflicting (same-named) file already exists in the target directory.\n\tskip:       skip conflicting files: leave them in the taildrop inbox and print an error. get any non-conflicting files\n\toverwrite:  overwrite existing file\n\trename:     write to a new number-suffixed filename"},{"Name":"loop","Desc":"run get in a loop, receiving files as they come in"},{"Name":"verbose","Desc":"verbose output"},{"Name":"wait","Desc":"wait for a file to arrive if inbox is empty"}]}]},{"Name":"bugreport","Desc":"Print a shareable identifier to help diagnose issues","Flags":[{"Name":"diagnose","Desc":"run additional in-depth checks"},{"Name":"record","Desc":"if true, pause and then write another bugreport"}]},{"Name":"cert","Desc":"Get TLS certs","Flags":[{"Name":"cert-file","Desc":"output cert file or \"-\" for stdout; defaults to DOMAIN.crt if --cert-file and --key-file are both unset"},{"Name":"key-file","Desc":"output key file or \"-\" for stdout; defaults to DOMAIN.key if --cert-file and --key-file are both unset"},{"Name":"min-validity","Desc":"ensure the certificate is valid for at least this duration; the output certificate is never expired if this flag is unset or 0, but the lifetime may vary; the maximum allowed min-validity depends on the CA"},{"Name":"serve-demo","Desc":"if true, serve on port :443 using the cert as a demo, instead of writing out the files to disk"}]},{"Name":"lock","Desc":"Manage tailnet lock","Subcommands":[{"Name":"init","Desc":"The 'tailscale lock init' command initializes tailnet lock for the\nentire tailnet. The tailnet lock keys specified are those initially\ntrusted to sign nodes or to make further changes to tailnet lock.\n\nYou can identify the tailnet lock key for a node you wish to trust by\nrunning 'tailscale lock' on that node, and copying the node's tailnet\nlock key.\n\nTo disable tailnet lock, use the 'tailscale lock disable' command\nalong with one of the disablement secrets.\nThe number of disablement secrets to be generated is specified using the\n--gen-disablements flag. Initializing tailnet lock requires at least\none disablement.\n\nIf --gen-disablement-for-support is specified, an additional disablement secret\nwill be generated and transmitted to Tailscale, which support can use to disable\ntailnet lock. We recommend setting this flag.","Flags":[{"Name":"confirm","Desc":"do not prompt for confirmation"},{"Name":"gen-disablement-for-support","Desc":"generates and transmits a disablement secret for Tailscale support"},{"Name":"gen-disablements","Desc":"number of disablement secrets to generate"}]},{"Name":"status","Desc":"Output the state of tailnet lock","Flags":[{"Name":"json","Desc":"output in JSON format"}]},{"Name":"add","Desc":"Add one or more trusted signing keys to tailnet lock"},{"Name":"remove","Desc":"Remove one or more trusted signing keys from tailnet lock","Flags":[{"Name":"re-sign","Desc":"resign signatures which would be invalidated by removal of trusted signing keys"}]},{"Name":"sign","Desc":"Either:\n  - signs a node key and transmits the signature to the coordination\n    server, or\n  - signs a pre-approved auth key, printing it in a form that can be\n    used to bring up nodes under tailnet lock\n\nIf any of the key arguments begin with \"file:\", the key is retrieved from\nthe file at the path specified in the argument suffix."},{"Name":"disable","Desc":"The 'tailscale lock disable' command uses the specified disablement\nsecret to disable tailnet lock.\n\nIf tailnet lock is re-enabled, new disablement secrets can be generated.\n\nOnce this secret is used, it has been distributed\nto all nodes in the tailnet and should be considered public."},{"Name":"disablement-kdf","Desc":"Compute a disablement value from a disablement secret (advanced users only)"},{"Name":"log","Desc":"List changes applied to tailnet lock","Flags":[{"Name":"json","Desc":"output in JSON format"},{"Name":"limit","Desc":"max number of updates to list"}]},{"Name":"local-disable","Desc":"The 'tailscale lock local-disable' command disables tailnet lock for only\nthe current node.\n\nIf the current node is locked out, this does not mean that it can initiate\nconnections in a tailnet with tailnet lock enabled. Rather, this means\nthat the current node will accept traffic from other nodes in the tailnet\nthat are locked out."},{"Name":"revoke-keys","Desc":"Retroactively revoke the specified tailnet lock keys (tlpub:abc).\n\nRevoked keys are prevented from being used in the future. Any nodes previously signed\nby revoked keys lose their authorization and must be signed again.\n\nRevocation is a multi-step process that requires several signing nodes to `--cosign` the revocation. Use `tailscale lock remove` instead if the key has not been compromised.\n\n1. To start, run `tailscale revoke-keys \u003ctlpub-keys\u003e` with the tailnet lock keys to revoke.\n2. Re-run the `--cosign` command output by `revoke-keys` on other signing nodes. Use the\n   most recent command output on the next signing node in sequence.\n3. Once the number of `--cosign`s is greater than the number of keys being revoked,\n   run the command one final time with `--finish` instead of `--cosign`.","Flags":[{"Name":"cosign","Desc":"continue generating the recovery using the tailnet lock key on this device and the provided recovery blob"},{"Name":"finish","Desc":"finish the recovery process by transmitting the revocation"},{"Name":"fork-from","Desc":"parent AUM hash to rewrite from (advanced users only)"}]}]},{"Name":"licenses","Desc":"Get open source license information"},{"Name":"exit-node","Desc":"Show machines on your tailnet configured as exit nodes","Subcommands":[{"Name":"list","Desc":"Show exit nodes","Flags":[{"Name":"filter","Desc":"filter exit nodes by country"}]},{"Name":"suggest","Desc":"Suggest the best available exit node"}]},{"Name":"update","Desc":"Update Tailscale to the latest/different version","Flags":[{"Name":"dry-run","Desc":"print what update would do without doing it, or prompts"},{"Name":"yes","Desc":"update without interactive prompts"}]},{"Name":"whois","Desc":"'tailscale whois' shows the machine and user associated with a Tailscale IP (v4 or v6).","Flags":[{"Name":"json","Desc":"output in JSON format"},{"Name":"proto","Desc":"protocol; one of \"tcp\" or \"udp\"; empty means both"}]},{"Name":"whoami","Desc":"'tailscale whoami' shows the machine and user identity of the current machine.\n\tIt is equivalent to running 'tailscale whois' against one of the current machine's own Tailscale IP addresses.","Flags":[{"Name":"json","Desc":"output in JSON format"}]},{"Name":"appc-routes","Desc":"The 'tailscale appc-routes' command prints the current App Connector route status.\n\nBy default this command prints the domains configured in the app connector configuration and how many routes have been\nlearned for each domain.\n\n--all prints the routes learned from the domains configured in the app connector configuration; and any extra routes provided\nin the policy app connector 'routes' field.\n\n--map prints the routes learned from the domains configured in the app connector configuration.\n\n-n prints the total number of routes advertised by this device, whether learned, set in the policy, or set locally.\n\nFor more information about App Connectors, refer to\nhttps://tailscale.com/kb/1281/app-connectors","Flags":[{"Name":"all","Desc":"print learned domains and routes and extra policy configured routes."},{"Name":"map","Desc":"print the map of learned domains: [routes]."},{"Name":"n","Desc":"print the total number of routes this node advertises."}]},{"Name":"wait","Desc":"Wait for Tailscale resources to be available. As of 2026-01-02, the only\nresource that's available to wait for by is the Tailscale interface and its\nIPs.\n\nWith no arguments, this command will block until tailscaled is up, its backend is running,\nand the Tailscale interface is up and has a Tailscale IP address assigned to it.\n\nIf running in userspace-networking mode, this command only waits for tailscaled and\nthe Running state, as no physical network interface exists.\n\nA future version of this command may support waiting for other types of resources.\n\nThe command returns exit code 0 on success, and non-zero on failure or timeout.\n\nTo wait on a specific type of IP address, use 'tailscale ip' in combination with\nthe 'tailscale wait' command. For example, to wait for an IPv4 address:\n\n    tailscale wait \u0026\u0026 tailscale ip --assert=\u003cspecific-IP-address\u003e\n\nLinux systemd users can wait for the \"tailscale-online.target\" target, which runs\nthis command.\n\nMore generally, a service that wants to bind to (listen on) a Tailscale interface or IP address\ncan run it like 'tailscale wait \u0026\u0026 /path/to/service [...]' to ensure that Tailscale is ready\nbefore the program starts.","Flags":[{"Name":"timeout","Desc":"how long to wait before giving up (0 means wait indefinitely)"}]},{"Name":"completion","Desc":"To load completions:\n\nBash:\n\n\t$ source \u003c(tailscale completion bash)\n\n\t# To load completions for each session, execute once:\n\t# Linux:\n\t$ tailscale completion bash \u003e /etc/bash_completion.d/tailscale\n\t# macOS:\n\t$ tailscale completion bash \u003e $(brew --prefix)/etc/bash_completion.d/tailscale\n\nZsh:\n\n\t# If shell completion is not already enabled in your environment,\n\t# you will need to enable it.  You can execute the following once:\n\n\t$ echo \"autoload -U compinit; compinit\" \u003e\u003e ~/.zshrc\n\n\t# To load completions for each session, execute once:\n\t$ tailscale completion zsh \u003e \"${fpath[1]}/_tailscale\"\n\n\t# You will need to start a new shell for this setup to take effect.\n\nfish:\n\n\t$ tailscale completion fish | source\n\n\t# To load completions for each session, execute once:\n\t$ tailscale completion fish \u003e ~/.config/fish/completions/tailscale.fish\n\nPowerShell:\n\n\tPS\u003e tailscale completion powershell | Out-String | Invoke-Expression\n\n\t# To load completions for every new session, run:\n\tPS\u003e tailscale completion powershell \u003e tailscale.ps1\n\t# and source this file from your PowerShell profile.\n\nThe shell scripts and this help message have been adapted from the\nCobra project (https://cobra.dev, https://github.com/spf13/cobra)\nunder the Apache-2.0 license. Thank you for making these available.\n","Subcommands":[{"Name":"bash","Desc":"Generate bash shell completion script","Flags":[{"Name":"descs","Desc":"Include flag, subcommand, and other descriptions in completions"},{"Name":"flags","Desc":"Suggest flag completions with subcommands"}]},{"Name":"zsh","Desc":"Generate zsh shell completion script","Flags":[{"Name":"descs","Desc":"Include flag, subcommand, and other descriptions in completions"},{"Name":"flags","Desc":"Suggest flag completions with subcommands"}]},{"Name":"fish","Desc":"Generate fish shell completion script","Flags":[{"Name":"descs","Desc":"Include flag, subcommand, and other descriptions in completions"},{"Name":"flags","Desc":"Suggest flag completions with subcommands"}]},{"Name":"powershell","Desc":"Generate powershell completion script","Flags":[{"Name":"descs","Desc":"Include flag, subcommand, and other descriptions in completions"},{"Name":"flags","Desc":"Suggest flag completions with subcommands"}]}]}],"Flags":[{"Name":"socket","Desc":"path to tailscaled socket"}]}
